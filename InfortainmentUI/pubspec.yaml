name: aiconnectcar
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.5.0-18.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  firebase_core: ^2.0.0
  firebase_auth: ^4.0.0
  firebase_database: ^10.0.0
  flutter_tts: ^3.4.0
  url_launcher: ^6.3.0
  kakao_flutter_sdk_common: ^1.2.0
  kakao_flutter_sdk_navi: ^1.2.0
  geolocator: ^10.1.0
  google_maps_flutter: ^2.6.0
  permission_handler: ^11.3.1
  flutter_compass: ^0.8.0
  speech_to_text: ^6.6.0
  get: ^4.6.6
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/

